<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/hola.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <title>Document</title>

</head>
<body>
<form action="/" method="post">
    <div class="wrapper">
     
        <spa class="bg-animate"></spa>
        <spa class="bg-animate2"></spa>


    <div class="form-box login ">
       <h2 class="animation">Inicio de sesion</h2>
          <div class="input-box animation ">
             <input type="text" name="username" id="user" value="<% if(typeof valores != 'undefined') { %><%= valores.user %><% } %>">
             <label>Usuario</label>
             <i class='bx bxs-user'></i>
             <% if (typeof errors !== 'undefined') { %>
                <% if ( errors.user ) { %>
                        <div>
                            <h2 class="error" 
                                
                                style="font-family: 'Arial', Times, serif;
                                font: small-caps 200% serif;
                                font-size: 18px;
                                color: #ff0000;
                                position: relative;
                                top: -15x;

                                "><i class="fa-solid fa-circle-exclamation" style="color: #db0c0c;"></i> <%= errors.user %></h2>
                            </div>
                  <% } %>
                  <% } %>
          </div>

            <div class="input-box animation">
               <input type="password" name="password" id="pass" value="<% if(typeof valores != 'undefined') { %><%= valores.pass %><% } %>">
               <label>Contraseña</label>
               <i class='bx bxs-lock-alt'></i>
               <% if (typeof errors !== 'undefined') { %>
                <% if ( errors.pass ) { %>
                        <div>
                            <h2 class="error" 
                                
                                style="font-family: 'Arial', Times, serif;
                                font: small-caps 200% serif;
                                font-size: 18px;
                                color: #ff0000;
                                position: relative;
                                top: -15x;

                                "><i class="fa-solid fa-circle-exclamation" style="color: #db0c0c;"></i> <%= errors.pass %></h2>
                            </div>
                  <% } %>
                  <% } %>
            </div>
            <% if (typeof err !== 'undefined') { %>

                        <div>
                            <h2 class="error" 
                                
                                style="font-family: 'Arial', Times, serif;
                                font: small-caps 200% serif;
                                font-size: 18px;
                                color: #ff0000;
                                position: relative;
                                top: -15x;

                                "><i class="fa-solid fa-circle-exclamation" style="color: #db0c0c;"></i> <%= err %></h2>
                            </div>
                  <% } %>
                  

            <button type="submit" class="btn animation">Iniciar
            </button>
            <div class="logreg-link animation ">
                <p>No tienes una cuenta? <a href="/registrar" class="registrer-link">Crear Cuenta</a></p><br>
                <p> <a href="/recuperar_cuenta" class="registrer-link">Olvidaste tu Contraseña?</a></p>

            </div>
   </div>
   
<div class="info-text login">
    <h2>Bienvenido </h2>
    <p>Order Shop</p>
   </div>


 </div>
</form>
</body>
</html>